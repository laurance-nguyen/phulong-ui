{
  "name": "phulong-ui",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "clean": "turbo clean",
    "format": "biome format --write ./apps ./packages"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.4.1",
    "eslint": "^8.48.0",
    "prettier": "^3.0.3",
    "prettier-plugin-tailwindcss": "^0.5.4",
    "tsconfig": "workspace:*",
    "turbo": "latest"
  }
}